<Window x:Class="WpfHandGazeDistance.Views.ParametersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfHandGazeDistance.Views"
        mc:Ignorable="d"
        Title="ParameterList" Height="450" Width="547.751">
    <!--<Grid>

        <TextBlock Text="Pixel Threshold" HorizontalAlignment="Left" Margin="99,59,0,0" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <Slider Value="{Binding PixelThreshold}" 
                Minimum="0" Maximum="100000"
                HorizontalAlignment="Left" Margin="205,59,0,0" VerticalAlignment="Top" Width="207"/>
        <TextBox Text="{Binding PixelThreshold}"
            HorizontalAlignment="Left" Height="23" Margin="434,56,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="59"/>
        <Button Command="{Binding GoCommand}" Content="Button" HorizontalAlignment="Left" Margin="271,164,0,0" VerticalAlignment="Top" Width="75"/>
        
    </Grid>-->

    <DataGrid DataContext="{Binding ParametersViewModel}" ItemsSource="{Binding ParameterCollection}" 
            AutoGenerateColumns="False" Margin="65,42,110.4,52.2" GridLinesVisibility="None">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Parameter Name" Width="SizeToCells" IsReadOnly="True">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="Juii"></TextBlock>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Value" Width="SizeToCells" IsReadOnly="True">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Value}"></TextBox>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</Window>
